# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- Este proyecto ---
# El contenido EN CLARO nunca se sube: el repo es público y solo debe viajar
# public/letters.enc.json, que va cifrado.
#
# Se ignora TODO lo que hay dentro de content/ y se deja pasar solo lo que puede
# ser público. Antes se listaba carpeta por carpeta y una carpeta nueva se
# colaba en el repo sin que nadie se diera cuenta.
content/*
!content/pista.txt
# Config de Firebase y contraseñas locales
.env
.env.local
